//! Multi-agent dialog demo — two RagAgent instances conversing.

use anyhow::Result;

#[tokio::main]
async fn main() -> Result<()> {
    println!("ragrig-dialog — not yet implemented");
    Ok(())
}
