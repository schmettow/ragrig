[package]
name = "ragrig-query"
version = "0.1.0"
edition = "2024"
description = "Single-shot RAG query — index, search, generate"

[dependencies]
ragrig = { path = "../..", features = ["test-fixtures"] }
anyhow = "1"
tokio = { version = "1", features = ["full"] }
