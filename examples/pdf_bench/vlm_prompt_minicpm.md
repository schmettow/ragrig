Convert this document page to clean, structured Markdown. Follow these rules exactly:

**Reading order:** For multi-column layouts, read the left column from top to bottom first, then the right column from top to bottom. Do not interleave text from different columns.

**Structural fidelity:**
- Use `#` for the document title, `##` for section headings, `###` for subsections
- Preserve **bold** and *italic* text where the original has it
- Separate paragraphs with a blank line
- Use proper Markdown tables (`| ... |`) for tabular data
- Use `- ` for bullet lists, `1. ` for numbered lists
- Preserve all mathematical expressions and formulas exactly

**Content rules:**
- Transcribe text verbatim — do not summarize, paraphrase, or condense
- Include all figure captions, table captions, footnotes, and page numbers
- Include all author names, affiliations, and citation numbers exactly as shown
- If text is unreadable, output `[illegible]` rather than guessing

**Output format:** Raw Markdown only. No conversational text, no "Here is the output:" preamble, no code fences.
